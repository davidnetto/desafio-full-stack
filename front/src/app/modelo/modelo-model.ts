import { inject } from "@angular/core";

// montadora.model.ts

export interface Modelo {
    montadoraId: number;
    id: number;
    nome: string;
    motadoraId:string;
    // Outras propriedades da montadora, se houverem
  }
  

  